# Admin panel: set a strong password in .env.local (copy this file to .env.local first).
ADMIN_PASSWORD=lilHova0612!


# Adobe Fonts (for Proxima Nova). Get your kit ID from fonts.adobe.com: create a kit, add Proxima Nova, copy the ID from the embed URL (e.g. abc1xyz from https://use.typekit.net/abc1xyz.css).
# NEXT_PUBLIC_ADOBE_FONTS_KIT_ID=your-kit-id

# Optional: custom path for SQLite DB (defaults to ./data/guffs.db)
# DATABASE_PATH=./data/guffs.db

# Optional: custom path for uploads (defaults to ./public/uploads)
# UPLOADS_PATH=./public/uploads
